{
  "private": true,
  "name": "varde",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "tokens": "designsystemet tokens create && designsystemet tokens build",
    "lint": "biome lint",
    "lint:write": "biome check --write",
    "www": "pnpm build:packages && pnpm --filter www dev",
    "build:www": "pnpm build:packages && pnpm --filter www build",
    "build:packages": "pnpm --filter @digdir/varde build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module",
  "packageManager": "pnpm@12.4.0",
  "devDependencies": {
    "@biomejs/biome": "2.5.12",
    "@digdir/designsystemet": "1.21.0"
  }
}
